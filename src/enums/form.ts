export enum FieldType {
  TEXT = 'text',
  NUMBER = 'number',
  EMAIL = 'email',
  TEXTAREA = 'textarea',
  SELECT = 'select',
  SWITCH = 'switch',
  CHECKBOX = 'checkbox',
  MULTI_CHECKBOX = 'multicheckbox',
}
